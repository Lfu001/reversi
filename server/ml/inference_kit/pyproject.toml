[project]
name = "inference-kit"
version = "0.1.0"
description = "A reusable inference kit for ML models."
requires-python = ">=3.9"
dependencies = [
    "numpy",
]

[build-system]
requires = ["uv_build>=0.8.22,<0.9.0"]
build-backend = "uv_build"
